export { fetchData }; // };//   return response.json();//   const response = await fetch('https://api.example.com/data');// const fetchExternalData = async () => {// For absolute URLs to external APIs, no change is needed:};  return response.json();  const response = await fetch(getUrl(`api/${endpoint}`));const fetchData = async (endpoint) => {// For API calls to your own backend that are relative to the app pathimport { getUrl } from '../utils/urlHelper';
