---
// タグページへのアクセスを /tags/#tag 形式にリダイレクト
export const prerender = false;

export async function GET({ params, redirect }) {
  const tag = params.tag;
  return redirect(`/tags/#${tag.toLowerCase()}`);
}
---
